import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 30,
  },

  row: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 10,
  },
});

export default styles;
